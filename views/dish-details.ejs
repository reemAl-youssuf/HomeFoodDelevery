<!DOCTYPE html>
<html lang="ar" dir="rtl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>الوجبة</title>
    <link rel="stylesheet" href="/css/bootstrap.min.css" />
    <link rel="stylesheet" href="/css/bootstrap.min.css.map" />
    <link rel="stylesheet" href="/css/all.min.css" />
    <link rel="stylesheet" href="/css/dish.css" />
  </head>
  <body>
    <a href="\kitchens\<%=dish.kitchen_id._id%>\dishes">
    <div class="container mt-5">
      <button class="btn btn-secondary py-2 px-4">
        <i class="fas fa-arrow-right ms-2"></i>العودة إلى المطبخ
      </button></a>

      <section class="py-3">
        <div class="container">
          <div class="row gx-5">
            <aside class="col-lg-6 d-flex align-item-center">
              <img
                style="max-width: 100%; height: 70%; margin: auto"
                class="rounded-4 fit"
                src="/img/<%=dish.image_name%>"
                alt="<%= dish.title %>"
              />
            </aside>
            <main class="col-lg-6">
              <form action="/cart/add" method="POST">
              <div class="ps-lg-3">
                <h4 class="title text-dark mb-4 mt-3"><%= dish.title %></h4>
                <span class="text-muted">المطبخ: </span><%= dish.kitchen_id.name
                %>
                <br />
                <span class="text-muted">التصنيف:</span><%=
                dish.category_id.title %>
                <div class="mb-3">
                  <span class="text-muted">السعر:</span>
                  <span class="h6"> <%= dish.newprice %>$</span>
                </div>

                <p><%= dish.description %></p>

                <div class="d-flex flex-row my-3">
                  <div class="text-warning mb-1 me-2">
                    <% for (let i = 0; i < 5; i++) { %> <% if (i <
                    dish.dratings) { %>

                    <li class="fa fa-star"></li>

                    <% } else { %>
                    <li class="far fa-star"></li>
                    <% } %> <% } %>
                  </div>
                </div>
                <hr />
                     
                <div class="row">
                  <div class="col-md-4 col-6 mb-3">
                    <label class="mb-2 d-block">الكمية المطلوبة</label>
                    <div class="input-group mb-3" style="width: 170px">
                      <button
                        class="btn btn-white border border-secondary px-3 quantity-left-minus"
                        type="button"
                        id="button-addon1"
                        data-mdb-ripple-color="dark"
                      >
                        <i class="fas fa-minus"></i>
                      </button>
                      <input
                        name="amount"
                        id="quantity"
                        type="number"
                        class="form-control text-center border border-secondary"
                        min="1"
                        max="10"
                        value="1"
                      />
                      <button
                        class="btn btn-white border border-secondary px-3 quantity-right-plus"
                        type="button"
                        id="button-addon2"
                        data-mdb-ripple-color="dark"
                      >
                        <i class="fas fa-plus"></i>
                      </button>
                    </div>
                  </div>
                </div>
                <div class="d-flex flex-row my-2 " >

                   <a >
                    <input type="hidden" name="ref" value="/kitchens/<%=dish.kitchen_id._id%> %>/dishes/<%=dish._id %>/details">
                    <input type="hidden" name="title" value="<%= dish.title %> ">
                    <input type="hidden" name="newprice" value="<%= dish.newprice %>">
                    <input type="hidden" name="oldprice" value="<%= dish.oldprice %>">
                    <input type="hidden" name="disheId" value="<%= dish._id %>">
                    <input type="hidden" name="redirectTo" value="/">
                    <button type="submit" class="btn btn-success shadow-0">
                   <i class="me-2 fa fa-shopping-basket"></i> إضافة للسلة </button> 
                 
                 </a>
                  
                      

            
            </form>
              <form action="/addToFavorites" method="POST">
                  <input type="hidden" name="ref" value="/kitchens/<%=dish.kitchen_id._id%> %>/dishes/<%=dish._id %>/details">
                      <input type="hidden" name="dishId" value="<%=dish._id %>">
                       <button class="border border-secondary py-2 icon-hover px-4 m-auto me-2"  style="background: none; border: none;"  type="submit">
                     <i class="me-1 fa fa-heart fa-lg" ></i>  حفظ في المفضلة </button>
                      </form>  </div>
            </main>  
          </div>
        </div>
      </section>
    </div>

    <script src="/js/all.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js"></script>
    <script src="/js/bootstrap.bundle.min.js.map"></script>
    <script src="/js/dish.js"></script>
  </body>
</html>
